[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --force --legacy-peer-deps --ignore-scripts"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node .output/server/index.mjs"
